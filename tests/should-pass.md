# Fixtures that must produce zero alerts

Ordinary technical and editorial prose. Any alert on this file is a false
positive and fails the suite. Every phrase below tripped a rule at some point;
each one is legitimate usage.

## Product and proper nouns

The iOS client is written in Swift and stores records in Realm.

The office is in Foster City. Route escalations to Dr. Foster.

Beacon discovery runs over the Web Bluetooth API.

## Technical vocabulary that overlaps slop words

Rename the file with an underscore instead of a hyphen.

The test harness the team wrote runs nightly against staging.

Users navigate the settings screen by tapping Back.

Lock the view to landscape orientation before recording.

The legacy endpoint returns a myriad value for older clients.

Set the realm attribute on the WWW-Authenticate header.

Call unlock on the mutex once the write completes.

Operators elevate privileges through the audit path, never directly.

Boast is a column name inherited from the 2019 schema.

## Attribution that names its source

Chen and Nakamura (2024, MIT) report a 40% drop in retention.

## Question acknowledgements that are ordinary prose

The design raises an interesting question about caching.

We used a good question from the survey.

That leaves one excellent question unanswered.

## Sentence shapes that are fine

The parser handles three cases — nested lists being the awkward one.

Moreover, the cache warms on boot. Furthermore, it survives restarts.

The migration is very fast and really quite simple to roll back.

We shipped it. That decision held up.
