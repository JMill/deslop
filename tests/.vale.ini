# Config for the rule test suite only. Fixtures are linted against the styles in
# this working tree (not a synced release), so a rule change is testable before
# it ships.
StylesPath = ../styles
MinAlertLevel = suggestion

[*.md]
BasedOnStyles = Deslop
